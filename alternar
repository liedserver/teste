function simularCtrlTab() {
    var evento = new KeyboardEvent('keydown', {
        key: 'Tab',
        ctrlKey: true,
    });

    document.dispatchEvent(evento);
}

// Chama a função para simular Ctrl + Tab a cada 5 segundos (5000 milissegundos)
setInterval(simularCtrlTab, 5000);
